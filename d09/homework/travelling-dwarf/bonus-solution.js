if ((d.orientation === "right") || (g.at(d.x + 1, d.y) === 0)) {
  d.orient("right");
  d.move();
}

// else if (g.at(d.x, d.y + 1) === 0) {
//   d.orient("down");
//   d.move();
// }

// else if (g.at(d.x - 1, d.y) === 0) {
//   d.orient("left");
//   d.move();
// }

// else (g.at(d.x, d.y - 1) === 0) {
//   d.orient("up");
//   d.move();
// }
// } else if (g.at(d.x, d.y + 1) === 0) {
//   d.orient("down");
//   d.move();
// } else if (g.at(d.x - 1, d.y) ===0) {
//   d.orient("left");
//   d.move();
// } else (g.at(d.x, d.y - 1) === 0) {
//   d.orient("up");
//   d.move();


// for ((d.orientation === "right") || (g.at(d.x + 1, d.y) === 0)) {
//   d.orient("right");  
//   d.move();
// } 

// else if (g.at(d.x, d.y + 1) === 0) {
//   d.orient("down");
//   d.move();
// } else if (g.at(d.x - 1, d.y) === 0) {
//   d.orient("left");
//   d.move();
// } else (g.at(d.x, d.y - 1) === 0) {
//   d.orient("up");
//   d.move();
// }

// do {d.orient("right")
// }
  
//   while (g.at(d.x + 1, d.y) === 0)

// do {d.orient("down")
// }
  
//   while (g.at(d.x, d.y + 1) === 0)

// do {d.orient("left")
// }
  
//   while (g.at(d.x - 1, d.y) === 0)

// do {d.orient("up")
// }
  
//   while (g.at(d.x, d.y - 1) === 0)


// else (g.at(d.x, d.y + 1) === 0) {
//   d.orient("down");
// } 
// else if (g.at(d.x - 1, d.y) === 0) {
//   d.orient("left");
// } else (g.at(d.x, d.y - 1) === 0) {
//   d.orient("up");
// }


// for (d.orientation === "right") 
  
//   if (g.at(d.x + 1, d.y) === "0") {
//   d.move();
// }
// }

// do {
//   d.move();
// }
// while ((g.at(d.x + 1, d.y) === "0") || (d.orientation === "right")){
//   d.move();
// }


// while (d.orientation === "right") {
//   if ((g.at(d.x + 1, d.y) === "0") || d.orientation === "right") {
//   d.move();
// }
//   else if (g.at(d.x + 1, d.y) === "color grey") {
//     d.orient("down");
//   }
// }

// while ((d.orientation === "down") || (g.at(d.x, d.y +1)) === "0");
// while ((d.orientation === "left") || (g.at(d.x - 1, d.y)) === "0");
// while ((d.orientation === "up") || (g.at(d.x, d.y - 1)) === "0");

// while ((d.orientation === "down") || (g.at(d.x, d.y + 1)) === "0") {
//   d.move();

//   if (g.at(d.x, d.y + 1) === "color grey") {
//   d.orient("left");
// }
// }

// while ((d.orientation === "right") || (g.at(d.x + 1, d.y)) === "0") {
//   d.move();

//   if (g.at(d.x + 1, d.y) === "0") {
//   d.orient("right");
// } else if (g.at(d.x + 1, d.y) === "color grey") {
//   d.orient("down");
// }
// }

// while ((d.orientation === "down") || (g.at(d.x, d.y + 1)) === "0") {
//   d.move();

//   if (g.at(d.x, d.y + 1) === "0") {
//   d.orient("down");
// } else if (g.at(d.x, d.y + 1) === "color grey") {
//   d.orient("right");
// }
// }

// while ((d.orientation === "down") || (g.at(d.x, d.y + 1)) === "0") {
//   d.move();

//   if (g.at(d.x, d.y + 1) === "color grey") {
//   d.orient("right");
// }
// }

// while ((d.orientation === "up") || (g.at(d.x, d.y - 1)) === "0") {
//   d.move();

//   if (g.at(d.x, d.y - 1) === "color grey") {
//   d.orient("right");
// }
// }
// if (g.at(d.x + 1, d.y) === "color grey") {
//   d.orient("down");
// } 

// while (g.at(d.x, d.y + 1) === 0) {
//   d.move();
// }